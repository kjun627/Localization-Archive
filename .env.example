OPENALEX_API_KEY=your_openalex_key
# Optional. Semantic Scholar can be queried without a key, but authenticated usage is
# still useful for higher or more predictable rate limits.
SEMANTIC_SCHOLAR_API_KEY=
